kind: pipeline
name: Kernel Drone

clone:
  depth: 1

steps:
 - name: compile
   image: ubuntu:latest
   environment:
     BOT_API_KEY:
       from_secret: BOT_API_KEY
     CHAT_ID:
       from_secret: CHAT_ID
   commands:
      - cd /drone/src
      - bash builder.sh
